<% if @items.size > 0 %>
  <% @items.each do |item| %>
    <%= render 'order_items/order_item', item: item %>
  <% end %>
  <hr>
  <div id="total-price" class="">
    Total price: <%= @items.each.sum { |i| i.product.price * i.quantity } %> UAH
  </div>
<% else %>
  Cart is Empty
<% end %>
